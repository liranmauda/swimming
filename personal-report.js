// per event, get the first 3 places
// per event get the first 3 times
// Per event get the times by order
// Do a +- range
// Get the official times of the first/last names in each event on that year till the date of the event
// compare with a list of predefined names (and corresponding name file)
// Convert into a csv (can we do a tab?) 